{
  "name": "backend",
  "version": "1.0.0",
  "description": "Hosting server locally for now. Must start the server so that the API calls can be made",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon --watch Algorithm --ext ts --exec ts-node app.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "node dist/app.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "cross-env": "^7.0.3",
    "nodemon": "^3.0.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
